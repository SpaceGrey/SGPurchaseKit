# SGPurchaseKit

SGPurchaseKit is a lightweight StoreKit 2 purchase layer for Apple platforms. It manages product loading, purchase state updates, restore flow, and local fallback cache for subscriptions and non-consumables.

The host app that uses this package defaults to `SGPurchases.fallbackPolicy = .days(14)`.
